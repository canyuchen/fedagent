# 1-GPU TASK-heterogeneity check: preference (content-dependent goal partition by category, omega skew).
# 2 clients / 1 round / 0.5B. Confirms the service partitions clients DIFFERENTLY (per-client
# [webshop-service] catalog_split client i/N log line) AND that training runs on the skewed shard.
env_kind: webshop
env_spec: config/envs/webshop_15.yaml
val_env_spec: ""
output_dir: /tmp/xbb9020_het_preference
model_path: /projects/b1222/.cache/huggingface/hub/models--Qwen--Qwen2.5-0.5B-Instruct/snapshots/7ae557604adf67be50417f59c2c2f167def9a775

total_clients: 2
clients_per_round: 2
total_rounds: 1
epochs_per_round: 1
base_seed: 42

n_gpus_per_node: 1
total_training_steps: 1
save_freq: 100000
test_freq: -1
wait_between_clients: 8
min_goals_per_client: 100
partition_strategy: preference
omega: 0.5
search_return_n: 200
webshop_pool_size: 8
webshop_base_port: 9500

client_overrides:
  - data.train_batch_size=8
  - data.max_prompt_length=2048
  - data.max_response_length=4096
  - actor_rollout_ref.actor.ppo_mini_batch_size=8
  - actor_rollout_ref.rollout.n=8
  - actor_rollout_ref.rollout.prompt_length=2048
  - actor_rollout_ref.rollout.response_length=4096
  - actor_rollout_ref.rollout.max_model_len=6144
  - actor_rollout_ref.rollout.gpu_memory_utilization=0.6
