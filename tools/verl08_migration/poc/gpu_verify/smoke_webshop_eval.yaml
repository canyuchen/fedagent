# EVAL-PATH smoke: the paper's headline metric path, never verified. val_env_spec set + test_freq=1
# + val_before_train -> the ALWAYS-ON unperturbed val service scores the GLOBAL model (base @ round0
# and the aggregated model @ round1). Confirms: val service starts on webshop_val_port (disjoint from
# the client band), verl validation runs, summarize_val_dump reads success_rate. 1.5B / 4-GPU.
env_kind: webshop
env_spec: config/envs/webshop_15.yaml
val_env_spec: config/envs/webshop_15_val.yaml
output_dir: /tmp/xbb9020_smoke_eval
model_path: /projects/b1222/.cache/huggingface/hub/models--Qwen--Qwen2.5-0.5B-Instruct/snapshots/7ae557604adf67be50417f59c2c2f167def9a775

total_clients: 2
clients_per_round: 2
total_rounds: 1
epochs_per_round: 1
base_seed: 42

n_gpus_per_node: 4
total_training_steps: 1
save_freq: 100000
test_freq: 1
val_before_train: true
val_temperature: 0.4
wait_between_clients: 8
min_goals_per_client: 100
partition_strategy: ""
search_return_n: 50
webshop_pool_size: 8
webshop_base_port: 8400
webshop_val_port: 8500

client_overrides:
  - data.train_batch_size=8
  - data.max_prompt_length=2048
  - data.max_response_length=6144
  - actor_rollout_ref.actor.ppo_mini_batch_size=8
  - actor_rollout_ref.rollout.n=8
  - actor_rollout_ref.rollout.prompt_length=2048
  - actor_rollout_ref.rollout.response_length=6144
  - actor_rollout_ref.rollout.max_model_len=8192
  - actor_rollout_ref.rollout.gpu_memory_utilization=0.6
