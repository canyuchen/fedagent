# Lever-#4 A/B CONTROL arm: subprocess-per-client (current path). 2 clients x 1 round, concat,
# 0.5B, 4 GPU, 2 steps. base_seed=42 -> round-1 client seeds = 42+100+{0,1} = 142/143, matching
# the persistent plan's seeds, so per-client checkpoints are directly comparable. cleanup off so
# compare_fsdp_checkpoints.py can diff the shards against the persistent arm.
model_path: ""
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/_scratch/accel/sub_out
rollout_mode: concat
total_clients: 2
clients_per_round: 2
total_rounds: 1
epochs_per_round: 1
base_seed: 42
n_gpus_per_node: 4
total_training_steps: 2
save_freq: 2
wait_between_clients: 5
cleanup_checkpoints: false
