# Reduced ALFWorld val spec for the eval-mode sweep (48 of the 140 valid_seen games) — large enough
# to surface the "shared throttles at large val" effect, small enough to keep a 4-mode sweep tractable.
# Full 274-game + per-task-type breakdown: tools/verl08_migration/eval_alfworld_by_tasktype.py.
envs:
  - name: ALFWorld
    n_envs: 48
    max_turns: 50
    agent_name: gym_text
    config:
      timeout: 180.0
