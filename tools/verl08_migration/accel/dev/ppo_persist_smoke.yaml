# Lever-#4 PPO smoke: persistent trainer with adv_estimator=gae -> exercises reload_critic_model
# (client 1's per-client reset reloads BOTH actor and critic). 2 clients x 1 round, concat, 0.5B,
# 4 GPU, 2 steps. Closing rc=0 (actor + critic FedAvg'd) validates the critic reload path.
model_path: ""
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/_scratch/accel/ppo_persist_out
rollout_mode: concat
persistent: true
adv_estimator: gae
total_clients: 2
clients_per_round: 2
total_rounds: 1
epochs_per_round: 1
base_seed: 42
n_gpus_per_node: 4
total_training_steps: 2
save_freq: 2
cleanup_checkpoints: false
