# Lever-#4 FULL-LOOP A/B, persistent arm: persistent=true (per-round in-process training).
# 2 clients x 2 rounds, GRPO, concat, 0.5B, 4 GPU, 2 steps. cleanup off so the final aggregated
# actor shards survive for equivalence diff vs the subprocess arm. base_seed=42 -> identical
# per-(round,client) seeds in both arms, so the whole loop is directly comparable.
model_path: ""
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/_scratch/accel/persist_full_out
rollout_mode: concat
persistent: true
total_clients: 2
clients_per_round: 2
total_rounds: 2
epochs_per_round: 1
base_seed: 42
n_gpus_per_node: 4
total_training_steps: 2
save_freq: 2
cleanup_checkpoints: false
