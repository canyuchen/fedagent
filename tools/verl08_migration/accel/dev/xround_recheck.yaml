# Lever-#4 EXTENDED A/B, cross-round arm: cross_round=true (ONE process spans BOTH rounds).
# Same shape/seeds as persist_full.yaml + subproc_full.yaml (2 clients x 2 rounds, GRPO, concat,
# 0.5B, 4 GPU, 2 steps, base_seed=42) so the final aggregated actor is directly comparable to
# BOTH the per-round-persistent and the subprocess arms. cleanup off so the round_2 shards survive
# for the equivalence diff. Expect: ONE cold-start total (vs 2 for per-round, 4 for subprocess).
model_path: ""
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/_scratch/accel/xround_recheck_out
rollout_mode: concat
cross_round: true
total_clients: 2
clients_per_round: 2
total_rounds: 2
epochs_per_round: 1
base_seed: 42
n_gpus_per_node: 4
total_training_steps: 2
save_freq: 2
cleanup_checkpoints: false
