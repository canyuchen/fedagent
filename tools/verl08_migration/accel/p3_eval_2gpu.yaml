# Eval job for the COMPLETE "2 client x 1 GPU + 2 GPU eval" test. A standalone verl val-only
# pass (driven through run_fed._build_eval -> the SAME code path the federated loop's eval uses),
# pinned to GPU 2,3, scoring the base 1.5B on the unperturbed val set while clients A/B train on
# GPU 0/1. Goal: measure whether eval HIDES under 1-GPU training + 4-GPU coexistence (3 jobs).
env_kind: webshop
env_spec: config/envs/webshop_15.yaml
val_env_spec: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/tools/verl08_migration/accel/webshop_val_64.yaml
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/_scratch/accel/p3_eval_2gpu_out
model_path: /projects/b1222/.cache/huggingface/hub/models--Qwen--Qwen2.5-1.5B-Instruct/snapshots/989aa7980e4cf806f80c7fef2b1adb7bc71aa306
total_clients: 1
clients_per_round: 1
total_rounds: 1
base_seed: 42
n_gpus_per_node: 2
webshop_pool_size: 16
webshop_base_port: 14000
webshop_val_port: 14090
search_return_n: 50
val_temperature: 0.4
client_overrides:
  - data.max_prompt_length=4096
  - data.max_response_length=512
  - actor_rollout_ref.rollout.n=8
  - actor_rollout_ref.rollout.prompt_length=4096
  - actor_rollout_ref.rollout.response_length=512
  - actor_rollout_ref.rollout.max_model_len=4608
  - actor_rollout_ref.rollout.gpu_memory_utilization=0.5
