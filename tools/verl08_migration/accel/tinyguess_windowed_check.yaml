# Windowed-default crash-fix check: rollout_mode defaults to WINDOWED (NOT overridden to concat).
# This is exactly the config shape that crashed earlier with
#   AttributeError: 'GymTextAgentLoop' has no attribute 'run_episode_windowed'
# before agent.yaml registered gym_text_windowed + the windowed manager auto-mapped agent_name.
# Expectation now: runs clean (windowed manager -> gym_text_windowed -> run_episode_windowed).
model_path: ""
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/_scratch/accel/windowed_check_out
# rollout_mode omitted -> default "windowed"
total_clients: 2
clients_per_round: 2
total_rounds: 1
epochs_per_round: 1
base_seed: 42
n_gpus_per_node: 4
total_training_steps: 2
save_freq: 2
cleanup_checkpoints: false
