# Cross-round + PPO: cross_round=true AND adv_estimator=gae -> exercises reload_critic_model in the
# cross-round reset path (critic federated across BOTH rounds in ONE process). 2cl x 2rd, 0.5B, 4 GPU.
model_path: ""
output_dir: /gpfs/projects/b1222/userdata/canyu/kangyu/fedagent/_scratch/accel/xround_ppo_out
rollout_mode: concat
cross_round: true
adv_estimator: gae
total_clients: 2
clients_per_round: 2
total_rounds: 2
epochs_per_round: 1
base_seed: 42
n_gpus_per_node: 4
total_training_steps: 2
save_freq: 2
cleanup_checkpoints: false
